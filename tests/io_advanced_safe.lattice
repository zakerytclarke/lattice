external function main() {
    let path: String(20) = "tests/hello.txt";
    
    let content_opt = read_file[20, 20](path);
    
    match (content_opt) {
        Some(content) => {
            print_string(content);
            return 1;
        }
        None => {
            return 0;
        }
    }
}
