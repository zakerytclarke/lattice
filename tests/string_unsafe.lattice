function get_char[MaxLen: Integer](s: String[MaxLen], idx: Integer) -> Char {
    idx >= 0 && idx < s.len;
} {
    return s.data.data[idx];
}

external function main() {
    let s: String[5] = String(List(['h', 'e', 'l', 'l', 'o']), 5);
    const c = get_char(s, 5);
    return 0;
}
